const wrapperFunction = () => {
  const prices = [10, 15, 20, 25];

  const logPrices = () => {
    console.log(prices);
  };

  logPrices();

  //   console.log(prices);
};

wrapperFunction();

// console.log(prices);
